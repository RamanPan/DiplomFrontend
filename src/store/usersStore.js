import {types} from 'mobx-state-tree';

const User = types.model('User',{});

const ActualUser = types.model('ActualUser',{})

const UsersStore = types.model('UsersStore',{


});

export default UsersStore;